# Development dependencies for Demonology CLI (updated)
-r requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# Code quality
black>=24.4.0
flake8>=7.0.0
mypy>=1.10.0
isort>=5.13.0
ruff>=0.4.0  # optional, fast linter

# HTTP testing
httpx[test]>=0.27.0   # match core httpx
responses>=0.25.0     # requests mocking
respx>=0.21.0         # httpx mocking

# Optional dependencies for testing
praw>=7.7.1           # reddit (if used)
yara-python>=4.4.0    # RE tests (if PatternSearchTool[yara])

# Additional development tools
pre-commit>=3.5.0
twine>=5.0.0
build>=1.2.1
